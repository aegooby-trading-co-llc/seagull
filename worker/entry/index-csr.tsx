import * as React from "react";

import { handlers } from "../handlers.js";
import { HTMLTemplate } from "../htmlTemplate.jsx";

export default handlers({
    reactElement: <HTMLTemplate element={null} />
});
